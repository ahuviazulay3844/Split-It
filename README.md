# SplitIn

פרויקט Node.js בסיסי שמתחבר ל-MongoDB.

התקנה והרצה:

```bash
npm install
cp .env.example .env
# עדכן את .env לפי הצורך
npm run dev
```

קבצים חשובים:
- `src/app.js` - נקודת כניסה
- `src/config/db.js` - חיבור ל-MongoDB
- `package.json` - סקריפטים ותלויות
